package StringDemo;

public class StringBuilderDemo {
    public StringBuilderDemo() {
        //mutable
        //not thread safe (it's method are non synchronous)
        StringBuilder stringBuilder = new StringBuilder();
    }
}
